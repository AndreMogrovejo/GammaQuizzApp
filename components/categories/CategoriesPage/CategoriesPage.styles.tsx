import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    maxHeight: "78%",
  },
  contentContainer: {
    padding: 16,
  },
});

export default styles;
